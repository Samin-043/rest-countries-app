const sum=(a,b)=>{
    const sum=a+b;
    return sum;
}

const subtract=(a,b)=>{
    const sub=a-b;
    return sub;
}

const division=(a,b)=>{
    const division=a/b;
    return division;
}

const multiply=(a,b)=>{
    const mul=a*b;
    return mul;
}

export {sum,subtract,division,multiply};

